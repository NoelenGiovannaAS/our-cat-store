import styled from 'styled-components';

const ProductsListWrapper = styled.div`
  background-color: blue;
`;

const ProductList = () => {
  return <ProductsListWrapper>Teste</ProductsListWrapper>;
};

export default ProductList;
